---
export interface Props {
  yPos: string;
  xPos: string;
}

const { yPos, xPos } = Astro.props;
---

<img
  src="assets/wave.png"
  alt="wave background"
  class={`w-[40rem] h-[40rem] absolute ${xPos} ${yPos} rotate-90`}
/>
