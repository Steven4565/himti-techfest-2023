---
export interface Props {
  yPos: string;
  xPos: string;
}

const { yPos, xPos } = Astro.props;
---

<img
  src="assets/aura.png"
  alt="aura background"
  class={`w-full absolute ${xPos} ${yPos}`}
/>
