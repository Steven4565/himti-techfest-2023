---
export interface Props {
  rotationClass: string;
  xPos: string;
}

const { rotationClass, xPos } = Astro.props;
---

<div
  class={`w-[40rem] h-[40rem] bg-gradient-to-r absolute ${xPos} -top-32 inline-block rounded-[7rem] ${rotationClass} to-[#022d46] from-[#934b8f] -z-10`}
>
</div>
